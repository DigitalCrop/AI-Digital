server { listen 80; root /usr/share/nginx/html; location = /health { access_log off; return 200 'ok'; add_header Content-Type text/plain; } location / { try_files $uri $uri/ /index.html; } location ~* remoteEntry.js$ { add_header Cache-Control 'no-store, max-age=0'; } }
